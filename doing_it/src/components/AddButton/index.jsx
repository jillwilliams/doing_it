import React from 'react';


// WHEN THE + BOX GETS CLICKED, THE WINDOW SHOWS AN INCREASE
// OF +1 FOR EACH CLICK
const addTocart = ( ) => {
    var count = document.getElementById("itemCountBox").value;
    document.getElementById("addItemToCart").addEventListener("click", 
        addItemToCart);
    var count = increaseByOne = ( ) => {
        while (i = 0; i < 6; i++) {
        let count = i;
        };
    addOne = count + 1;
    };
const addItemToCart = ( ) => {
    document.getElementById("itemCountBox").innerHTML= addToCart( );
    document.getElementById("itemCountBox").innerHTML= addOne( );
};

// WHEN THE SUBTRACT BUTTON IS CLICKED, THE ITEM COUNT BOX WILL
// DECREASE BY ONE EACH TIME THE BUTTON IS CLICKED
const subtractFromCart = ( )=> {
    document.getElementById("itemCountBox");
    document.getElementById("subtractItemFromCart").addEventListener("click", 
        subtractItemFromCart);
    var subtractOne = count - 1;

const subtractItemFromCart = ( ) => {
        document.getElementById("itemCountBox").innerHTML= subtractFromCart( );
        document.getElementById("itemCountBox").innerHTML= subtractOne( );
};